import { HourLabelPipe } from './hour-label-pipe';

describe('HourLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new HourLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
