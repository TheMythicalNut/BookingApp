import { ScrollIntoView } from './scroll-into-view';

describe('ScrollIntoView', () => {
  it('should create an instance', () => {
    const directive = new ScrollIntoView();
    expect(directive).toBeTruthy();
  });
});
